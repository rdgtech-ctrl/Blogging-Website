async (_, res)
The first parameter is req but it's written as _ — this is a convention meaning "I don't need req here, so I'm ignoring it".

The core logic:
javascriptres.status(200).cookie("token", "", { maxAge: 0 }).json({...})
It does 3 things chained together:

.status(200) — sends a success response
.cookie("token", "", { maxAge: 0 }) — clears the token cookie by setting its value to empty "" and maxAge: 0 which means it expires immediately
.json({...}) — sends back a success message

Vite is build tool and development server for frontend projects like React,Vue,etc
It's the tool that sets up, runs,and builds your frontend app.
Browsers don't understand JSX.Browsers only understand plain HTML,CSS and regular JavaScript.
So something needs to convert your React/JSX code into plain JavaScript that the browser can understand.That's what a build tool does.

### React - Router - Dom

React Router DOM lets you build multiple pages in a React app without actually reloading the browser.

Without React Router DOM:
Your app only has one page — everything is on the same screen.
With React Router DOM:
You can have different URLs showing different components, like:
/ → Home page
/login → Login page
/register → Register page
/profile → Profile page

```
import { BrowserRouter, Routes, Route } from "react-router-dom"
import Home from "./pages/Home"
import Login from "./pages/Login"

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/login" element={<Login />} />
      </Routes>
    </BrowserRouter>
  )
}
```

BrowserRouter — wraps your whole app, enables routing
Routes — container for all your routes
Route — maps a URL path to a component
Link — like <a> tag but without page reload
useNavigate — lets you redirect the user in code (e.g. after login)
useParams — lets you read URL parameters like /post/123

### Why jsx

So why JSX?

Much easier to read and write
Looks like HTML so it's familiar
Easier to visualize your UI structure
Less code to write

### dark:border-b-gray-600

dark: — applies this style only when dark mode is active
border-b — adds a border to the bottom only
gray-600 — sets the border color to gray shade 600 (a medium-dark gray)

### display: block

makes an element take up the full width of its container and start on a new line.

### flex-1

In simple terms — "take up all the remaining available space".

### cors

Your frontend runs on http://localhost:5173 and your backend runs on http://localhost:8000. These are different origins (different ports = different origin).
By default, browsers block requests from one origin to another for security reasons.So when your React app tries to call your Express API,the browser will block it

With cors package you fix it:
javascriptimport cors from 'cors'

app.use(cors({
origin: 'http://localhost:5173', // allow your frontend
credentials: true // allow cookies to be sent
}))
Now your backend says "I trust requests coming from localhost:5173" and the browser allows it.

credentials: true is important because you're using cookies (your JWT token is stored in a cookie). Without it, cookies won't be sent along with requests.

In short — cors is needed whenever your frontend and backend are on different ports or domains.

### console.log()

console.log() is a synchronous operation. Too many logs slow down your app, especially under heavy traffic.

### why inside redux we are using files named in .js not in .jsx

Because Redux files contain pure JavaScript logic — no JSX (no HTML-like syntax) at all.

.jsx is only needed when a file contains JSX syntax like:
jsxreturn (

  <div>
    <h1>Hello</h1>
  </div>
)

Redux files like store.js or authSlice.js only contain plain JavaScript:
javascriptimport { createSlice } from '@reduxjs/toolkit'

const authSlice = createSlice({
name: 'auth',
initialState: { user: null },
reducers: {
setUser: (state, action) => {
state.user = action.payload
}
}
})
No HTML, no components, no JSX — just plain JS objects and functions.

Simple rule:
File containsExtensionJSX (HTML-like syntax).jsxPure JavaScript logic.js
So components like Login.jsx, Signup.jsx, Navbar.jsx use .jsx because they return UI. But store.js, authSlice.js use .js because they only contain logic.

### { withCredentials: true }

This tells axios to include cookies in the request.This is important because:
Your JWT token is stored in a cookie
Without withCredentials:true, the browser won't send the cookie
The backend won't know which user is loggin out

###### Redux Toolkit

### authSlice.js

Manages who is logged in and loading state.

```
state.auth.user    → the logged in user object (or null)
state.auth.loading → true/false (show spinner or not)
```

Any component can read or update this — that's why Login.jsx uses:

```
useSelector(store => store.auth)       // read loading, user
dispatch(setLoading(true))             // update loading
dispatch(setUser(res.data.user))       // update user
```

### themeSlice.js

Manages light or dark mode.

```
state.theme.theme -> "light" or "dark"
```

Any component (like a toggle button in Navbar) can do:

```
dispatch(toggleTheme()) //switches between light and dark
```

### store.js

Combines both slices into one central store:

```
store
 ├── state.auth.loading
 ├── state.auth.user
 └── state.theme.theme
```

### ThemeProvider.jsx

Reads state.theme.theme and puts "light" or "dark" as a className on the outermost div. This is what activates Tailwind's dark: classes throughout your entire app.

```
<div className="dark">          ← when theme is "dark"
  <div className="dark:bg-[rgb(16,23,42)]">   ← this activates ✅
```

### How useSelector & useDispatch Work

How useSelector & useDispatch Work

useSelector
Read data from Redux store
Example:
const { user } = useSelector(state => state.auth)

useDispatch
Update data in Redux store
Example:
dispatch(setUser(data))

Think of it like:
useSelector = getter
useDispatch = setter

### isActive ?

t comes from React Router's NavLink component. It only works with NavLink, not a regular <Link>.

```
import { NavLink } from 'react-router-dom'

<NavLink
  to="/dashboard"
  className={({ isActive }) => `text-2xl ${isActive ? "bg-gray-800 text-gray-200" : "bg-transparent"}`}
>
  Dashboard
</NavLink>
```

### Cloudinary

Cloudinary is a cloud-based media managment service - it stores, manages,and delivers images and videos for your web app.

Without Cloudinary:

```
User uploads a profile photo
      ↓
You save it to your server's disk
      ↓
❌ Server storage fills up
❌ Slow delivery to users far away
❌ No image optimization
❌ Hard to manage/delete files
```

With Cloudinary:

```
User uploads a profile photo
      ↓
You send it to Cloudinary
      ↓
Cloudinary gives you back a URL
      ↓
You save that URL in your database
      ↓
✅ Fast delivery worldwide (CDN)
✅ Auto image optimization
✅ No server storage needed
```

### dotenv.config() does ?

it reads your .env file and loads all the variables into process.env so your code can access them.

### The full picture of redux files

authSlice.js → defines the box (state) and functions (setUser, setLoading)
store.js → combines all boxes into one warehouse
redux-persist → saves the warehouse to localStorage
dispatch(setUser()) → puts data into the box
useSelector() → reads data from the box in any component

### timestamps: true

automatically adds two fields to every document:

```
createdAt  // when the document was created
updatedAt  // when the document was last updated
```

### In createBlogHandler:

Reading from redux:
```
// somewhere at top of your component you have
const { blog } = useSelector((state) => state.blog)
//                                          👆 reading blog from global box
```
 
Writing to redux:
```
dispatch(setBlog([...blog, res.data.blog]))
//       👆 setBlog is the function from blogSlice
//          that updates the global blog box
```

Why [...blog, res.data.blog]?
```
// current global state
blog = [blog1, blog2]

// new blog from backend
res.data.blog = { title: "My Blog", category: "Tech", _id: "64f..." }

// spread old blogs + add new one
[...blog, res.data.blog] = [blog1, blog2, newBlog]

// now global state is updated
blog = [blog1, blog2, newBlog]  ✅
```

Why store in redux at all?
Because multiple components need blog data:

```
CreateBlog page   → needs blog list
YourBlogs page    → needs blog list
Sidebar           → might show blog count
```
Instead of fetching from backend every time → store once in redux, use everywhere!
The full picture:
```
blogSlice.js          → defines the box (blog, loading)
setBlog               → function to update the box
dispatch(setBlog(...)) → actually updates the box
useSelector           → reads from the box in any component
```

### useParams Hook
useParams is a React Router hook that lets you read dynamic URL parameters from the current route.

###
```
await Blog.find({ author: userId })
```
Blog.find() -> search the Blog collection in MongoDB
{author:userId} -> find only blogs where author matches his userId
await -> wait for the database to respond


Normally author field just stores an ID like "64f1a2b3c4d5e6f7"
.populate() → replaces that ID with the actual user data
path: "author" → which field to populate
select: "firstName lastName photoUrl" → only bring these 3 fields, ignore the rest (like password, email etc.)

### All HTTP Methods Compared:
```
MethodUse caseExampleGETFetch dataGet a blogPOSTCreate newCreate a blogPUTReplace entire objectUpdate whole blogPATCHUpdate one field onlyToggle publish statusDELETEDeleteDelete a blog
```